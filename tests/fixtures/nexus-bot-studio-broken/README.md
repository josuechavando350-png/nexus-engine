# Nexus Bot Studio — permanent broken-build fixture

This fixture is intentionally broken and must never be removed or converted to
a passing example. The geometric comparator loads `index.html` and must return
`FAIL`; a green result is a comparator regression.

Typography mixing, identical columns and focus treatment are evaluated against
structured `design-dna.json`, not inferred from geometric thresholds.
