{
  "name": "@nexus/edge-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cargo install -q worker-build && worker-build --release",
    "format": "cargo fmt -- --check",
    "check": "cargo check --target wasm32-unknown-unknown",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
