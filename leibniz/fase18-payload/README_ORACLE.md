# Oracle

The independent finite-model oracle enumerates all extensional functions for bounded standard models and cross-checks eleven additional Rust tests. A passing finite sample never certifies validity on all domains, especially infinite ones. Rust failures must block the second-block CI release. No changes to `main` or live NEXUS integration.
