[package]
name = "nemesis-fhe"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tfhe = { version = "=1.8.1", default-features = false, features = ["boolean"] }
bincode = "=1.3.3"
serde = { version = "=1.0.228", features = ["derive"] }
sha2 = "=0.10.9"

[profile.release]
lto = "thin"
