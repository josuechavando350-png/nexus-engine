# GAUSS 1,000 integration staging

The historical `main` registry has 200 operators. The proposed 800 additional operators are currently in a separately verified local source bundle, **not** yet part of this branch. This file records the integration boundary; it is not a certification, nor a claim that the 1,000 operators have been committed. Nothing in this branch should be merged into `main` until every source file, WALLE independent replay, Quantum classical simulation, and the full CI are verified on the exact commit SHA. Do not deploy client sites from this staging branch.
