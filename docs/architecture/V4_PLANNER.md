# V4 Planner
Plans are versioned typed DAGs. Every node carries capability, policy context, expected effect, retry/timeout, reversibility and evidence. Cycles, missing dependencies and ungrounded nodes are rejected. Candidate scoring exists in code rather than prompt prose.
