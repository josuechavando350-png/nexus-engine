# V4 Memory
Memory is working + episodic + semantic + procedural + goal memory with provenance, validity and confidence. `MemoryStore` is the port; vector search is optional retrieval infrastructure, not the memory model.
