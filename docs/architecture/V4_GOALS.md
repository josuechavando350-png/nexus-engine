# V4 Persistent Goals
Goals use a closed state machine with bounded retry budgets and explicit cancellation/blocking. Illegal transitions fail closed. Long-lived execution state is separated from model conversations.
