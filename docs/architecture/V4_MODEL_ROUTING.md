# V4 Model Routing
`ModelProvider` and capability-based routing isolate provider/model choice from NEXUS cognition. Current repository ships a deterministic provider for replay/tests. Real providers must be adapters and must not gain execution authority.
