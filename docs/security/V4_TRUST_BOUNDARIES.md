# V4 Trust Boundaries
Untrusted: model output, retrieved memory payload, tool output, external provider responses, MCP servers, semantic indexes. Trusted only after validation: typed NEXUS contracts and provenance. Authority remains in V3 policy/approval/edge gates. Retrieval, models and agents never grant permissions.
