{
  "name": "@nexus/evidence",
  "version": "9.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts",
    "./signed-evidence-certification": "./signed-evidence-certification.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && test -f dist/evidence/index.js && test -f dist/evidence/index.d.ts && test -f dist/evidence/signed-evidence-certification.js && test -f dist/evidence/signed-evidence-certification.d.ts"
  }
}
