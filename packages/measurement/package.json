{
  "name": "@nexus/measurement",
  "version": "9.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts",
    "./visual-algebra": "./visual-algebra.ts",
    "./topology": "./topology.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && test -f dist/measurement/index.js && test -f dist/measurement/index.d.ts"
  },
  "dependencies": {
    "@nexus/topology": "workspace:*",
    "@nexus/visual-algebra": "workspace:*"
  }
}
