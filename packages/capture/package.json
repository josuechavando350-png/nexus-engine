{
  "name": "@nexus/capture",
  "version": "9.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts",
    "./playwright": "./playwright-adapter.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:browser": "vitest run tests/playwright-adapter.test.ts --testTimeout=120000",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && test -f dist/capture/index.js && test -f dist/capture/index.d.ts && test -f dist/capture/playwright-adapter.js && test -f dist/capture/playwright-adapter.d.ts"
  },
  "dependencies": {
    "@axe-core/playwright": "^4.10.2",
    "playwright": "^1.55.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "vitest": "^4.0.0"
  }
}
