{
  "name": "@nexus/quality",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts",
    "./red-team": "./red-team.ts",
    "./repair-loop": "./repair-loop.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && test -f dist/quality/index.js && test -f dist/quality/index.d.ts && test -f dist/quality/red-team.js && test -f dist/quality/repair-loop.js"
  },
  "dependencies": {
    "@nexus/capture": "workspace:*",
    "@nexus/creative": "workspace:*",
    "@nexus/experience": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "vitest": "^4.0.0"
  }
}
