{
  "name": "@nexus/crawl-observability",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./render-diff": "./src/render-diff.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. packages/crawl-observability/src/*.test.ts",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && test -f dist/index.js && test -f dist/index.d.ts && test -f dist/render-diff.js && test -f dist/render-diff.d.ts"
  }
}
