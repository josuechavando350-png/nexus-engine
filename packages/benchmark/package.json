{
  "name": "@nexus/benchmark",
  "version": "9.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && test -f dist/benchmark/index.js && test -f dist/benchmark/index.d.ts"
  }
}
