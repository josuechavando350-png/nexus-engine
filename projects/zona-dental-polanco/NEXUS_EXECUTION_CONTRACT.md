# Zona Dental Polanco — NEXUS execution contract

## Supreme instruction

The operator must not design this client site.

The operator is only an integrator, executor, technical supervisor and evidence custodian for NEXUS. All creative decisions belong to NEXUS.

Before any visual change, ask: **“Did this decision come explicitly from NEXUS?”** If not, do not make it. Return it to the engine.

## User mandate for this project

- Client: **Zona Dental Polanco**.
- Site: **multipage**.
- Contact: **5543432221**.
- Location: **Av. Ejército Nacional Mexicano 826a, Polanco, Polanco II Secc, Miguel Hidalgo, 11550 Ciudad de México, CDMX**.
- Reuse the engineering shell from the supplied archive where technically compatible.
- Visual family explicitly requested by the user: **white + elegant gray**.
- **Absolutely no gold** in the generated site.
- Desired brand feeling explicitly requested by the user: **boutique, professional**.
- The user expects NEXUS to push the creative quality as far as its gates permit; weak or generic output must not be approved.
- Previous assistant-generated visual proposals are rejected and must not be treated as references or Golden Targets.

## Prohibited operator behavior

Do not manually choose or author:

- layouts, grids, section order or page composition;
- typography, type scale, font choice or hierarchy;
- colors beyond preserving the user's explicit white/gray and no-gold constraint as engine input;
- spacing, margins, radii, shadows, borders or decorative effects;
- navigation treatment;
- hero/opening composition;
- cards or other presentation patterns;
- image placement/cropping/priority;
- motion, transitions, scroll choreography, WebGPU/WebGL effects;
- responsive art direction;
- content presentation structure;
- visual repair intended to “make it look better.”

Manual code is permitted only for neutral infrastructure: wiring, imports, mounting, adapters, runtime, configuration, security, accessibility plumbing, deployment and fixes that do not introduce an operator-authored visual decision.

## Required NEXUS sequence

Run, in this order, without bypass:

```text
BRIEF
→ EVIDENCE
→ EXPERIENCE DNA
→ DNA CONTENT CONSTRAINTS
→ CONTENT READINESS
→ CONSTRAINED GENERATION
→ EMITTER
→ RENDER
→ BROWSER CAPTURE
→ DESIGN GENOME
→ VISUAL JUDGE
→ RED TEAM
→ REPAIR LOOP
→ RECAPTURE
→ REJUDGE
→ DELIVERY CERTIFICATION
```

Creative generation must be **CONSTRAINED BY DNA**. Never generate the site first and rationalize it afterward.

## Creative Tournament

When the engine supports multiple valid directions, require a Creative Tournament. NEXUS must produce multiple candidates and the gates must determine which survives. The operator may not choose a favorite by taste.

Permitted verdicts only:

- `PASS`
- `FAIL`
- `WARNING`
- `NOT_TESTED`

Never invent numeric aesthetic scores.

## Visual Judge and evidence

The Visual Judge must evaluate rendered evidence, not source intent alone. Evidence should include, where available:

- real browser screenshots;
- 390 / 768 / 1440 viewport captures according to the quality-loop policy;
- Design Genome data;
- browser evidence;
- accessibility evidence;
- business constraints;
- ExperienceDNA;
- content constraints;
- versioned rubric/provenance.

If a real multimodal provider did not execute, record `NOT_TESTED`. Never simulate a judge run.

## Red Team

Before approval, execute the NEXUS attacks available in the branch, including applicable equivalents of:

- Brand Swap;
- Industry Transplant;
- Content Stress;
- Asset Degradation;
- Viewport Torture;
- Motion Removal;
- Grayscale;
- Template Fingerprint;
- AI Fingerprint;
- Excess Attack.

A site is not approved because it merely looks attractive. It must survive an active attempt to prove it generic, brittle or mediocre.

## Vision-guided repair

When a judge finds a problem, do not repair it manually. Send the finding back into NEXUS:

```text
RENDER
→ SCREENSHOT
→ JUDGE
→ FINDING
→ NEXUS REPAIR
→ REBUILD
→ SCREENSHOT
→ REJUDGE
```

Preserve revision, source SHA, lineage, artifacts, findings, repair action and post-repair result.

## Content integrity

Do not invent facts. In particular, do not fabricate:

- doctor identity or credentials;
- treatments not supported by supplied/verified evidence;
- awards;
- patient counts;
- years of experience;
- reviews/testimonials;
- rating counts;
- prices;
- hours;
- outcomes;
- before/after claims;
- conversion metrics.

Facts from external sources must be real and verifiable.

## Photography and assets

Use only:

- user/client-supplied photography;
- explicitly authorized assets;
- assets with known provenance/rights.

The operator does not decide which image “looks best.” NEXUS must determine image function/priority through the project pipeline.

## CSS and identity

Identity CSS must come from the NEXUS ExperienceDNA/emitter/token pipeline. Do not hand-author CSS to reach a desired appearance or manually imitate a reference.

## Capability-missing rule

If any required step cannot be executed by the current NEXUS branch:

1. Record exactly: `NEXUS_CAPABILITY_MISSING`.
2. Identify the missing capability precisely.
3. Implement or connect that capability **inside NEXUS**.
4. Add tests.
5. Re-run NEXUS.
6. Use only the new engine output.

Never substitute manual design work for a missing engine capability.

## Delivery gate

A successful build is not approval. Do not call the project finished, approved, certified, production-ready or ready for the client until the fail-closed Delivery Certification Gate passes.

The final project must truthfully support this statement:

> The AI did not design this page. The AI operated NEXUS. Creative decisions were produced, evaluated, repaired and approved through the engine.
