# Pinned so CI, developers and the release build agree byte for byte.
# Rust 1.88 is required by currently resolved dependencies.
# Raise deliberately, never implicitly.

[toolchain]
channel = "1.88.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
