[package]
name = "nexus-ontology"
description = "Entity/relationship model, temporal facts, provenance, storage ports and deterministic entity resolution. Contains no graph-database coupling."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nexus-event = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
