[package]
name = "nexus-observability"
description = "Structured logging, metrics, health probes and the append-only audit trail."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
nexus-event = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
