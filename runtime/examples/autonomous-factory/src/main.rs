#![forbid(unsafe_code)]
use nexus_event::Timestamp;use nexus_goal::Goal;use nexus_intelligence::IntelligenceEngine;use nexus_memory::{InMemoryMemoryStore,MemoryKind,MemoryRecord,MemoryStore};use nexus_planner::DeterministicPlanner;
fn main(){let now=Timestamp::from_millis(1_800_000_000_000);let mem=InMemoryMemoryStore::default();mem.put(MemoryRecord{id:"episode-1".into(),kind:MemoryKind::Episodic,created_at:now,valid_from:now,valid_until:None,source_refs:vec!["evt_factory_alarm_1".into()],ontology_refs:vec!["ent_press_4".into()],causal_refs:vec![],scope:"factory-line".into(),confidence:1.0,payload:"temperature anomaly observed".into(),embedding_ref:None}).expect("memory");let mut goal=Goal::new("goal-inspect-press-4","inspect press-4 after temperature anomaly","operations",now).expect("goal");goal.policy_scope="factory-line".into();let planner=DeterministicPlanner;let engine=IntelligenceEngine{memory:&mem,planner:&planner};let d=engine.prepare(&mut goal,now).expect("decision");assert!(d.requires_v3_dispatch_gate);println!("V4 cognitive decision {} prepared; V3 dispatch gate required",d.plan.id);}
